'use client';
import { useEffect } from 'react';
import { ArrowLeft, RefreshCcw } from 'lucide-react';
import Link from 'next/link';
export default function Error({ error, reset }: { error: Error & { digest?: string }; reset: () => void; }) { useEffect(() => { console.error(error); }, [error]); return (<main className="min-h-screen flex items-center justify-center bg-white p-6"><div className="max-w-lg rounded-2xl border border-zinc-200 bg-white p-8 shadow-soft"><p className="text-xs font-mono uppercase tracking-[0.24em] text-yellow-600">Unexpected error</p><h1 className="mt-3 text-2xl font-semibold text-zinc-900">Something went wrong</h1><p className="mt-2 text-sm text-zinc-600">Please retry. If the problem persists, return to the dashboard and reopen the job.</p><div className="mt-6 flex gap-3"><button onClick={reset} className="inline-flex items-center gap-2 rounded-xl bg-yellow-300 px-4 py-2 text-sm font-semibold text-zinc-900 hover:bg-yellow-400"><RefreshCcw className="h-4 w-4" />Retry</button><Link href="/jobs" className="inline-flex items-center gap-2 rounded-xl border border-zinc-200 px-4 py-2 text-sm font-medium text-zinc-900 hover:bg-zinc-50"><ArrowLeft className="h-4 w-4" />Back to jobs</Link></div></div></main>); }